# Agent Guidelines

## Git Operations
- **Do not automatically push or pull**: Never run `git push` or `git pull` automatically.
- Always wait for explicit instruction or confirmation from the user before pushing to or pulling from any remote repository.
